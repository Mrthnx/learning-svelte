# Arquitectura del Módulo Menu Management

## 📐 Diagrama de Arquitectura

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENTE (Frontend)                       │
└───────────────────────────┬─────────────────────────────────────┘
                            │ HTTP Requests (JWT)
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    EXPRESS MIDDLEWARE LAYER                      │
│  ┌──────────────────┐  ┌──────────────────┐                    │
│  │ Authentication   │  │   Pagination     │                    │
│  │   Middleware     │  │    Middleware    │                    │
│  └──────────────────┘  └──────────────────┘                    │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    MENU MANAGEMENT CONTROLLER                    │
│  /api/menu-management                                           │
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │
│  │   Roles     │  │ Permissions │  │   Options   │           │
│  │  Endpoints  │  │  Endpoints  │  │  Endpoints  │           │
│  │  (5)        │  │  (5)        │  │  (6)        │           │
│  └─────────────┘  └─────────────┘  └─────────────┘           │
│                                                                 │
│  ┌──────────────────────────────────────────────┐             │
│  │      Role Permissions Endpoints (6)          │             │
│  └──────────────────────────────────────────────┘             │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                   MENU MANAGEMENT SERVICE                        │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  Business Logic & Data Orchestration (24 methods)        │ │
│  │                                                           │ │
│  │  • Role Operations (5)                                   │ │
│  │  • Permission Operations (5)                             │ │
│  │  • Option Operations (6)                                 │ │
│  │  • Role Permission Operations (8)                        │ │
│  └──────────────────────────────────────────────────────────┘ │
└──────┬──────────────┬──────────────┬──────────────┬────────────┘
       │              │              │              │
       ▼              ▼              ▼              ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌────────────────────┐
│     Role     │ │  Permission  │ │    Option    │ │ RolePermission     │
│  Repository  │ │  Repository  │ │  Repository  │ │ OptionRepository   │
└──────┬───────┘ └──────┬───────┘ └──────┬───────┘ └──────┬─────────────┘
       │                │                │                │
       └────────────────┴────────────────┴────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      TypeORM Connection                          │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                         MySQL DATABASE                           │
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │
│  │   roles     │  │ permissions │  │   options   │           │
│  └─────────────┘  └─────────────┘  └─────────────┘           │
│                                                                 │
│  ┌──────────────────────────────────────────────┐             │
│  │       role_permission_options                │             │
│  └──────────────────────────────────────────────┘             │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Flujo de Datos

### Request → Response Flow

```
┌──────────┐
│ Cliente  │
└────┬─────┘
     │ 1. HTTP Request
     ▼
┌─────────────────┐
│ Authentication  │ 2. Validate JWT Token
│   Middleware    │
└────┬────────────┘
     │ 3. Request validated
     ▼
┌─────────────────┐
│   Controller    │ 4. Parse params, call service
└────┬────────────┘
     │ 5. Service call
     ▼
┌─────────────────┐
│    Service      │ 6. Business logic
└────┬────────────┘
     │ 7. Repository calls
     ▼
┌─────────────────┐
│   Repository    │ 8. Database query
└────┬────────────┘
     │ 9. Entity data
     ▼
┌─────────────────┐
│     Mapper      │ 10. Transform to ViewModel
└────┬────────────┘
     │ 11. ViewModel
     ▼
┌─────────────────┐
│   Controller    │ 12. HTTP Response
└────┬────────────┘
     │ 13. JSON Response
     ▼
┌──────────┐
│ Cliente  │
└──────────┘
```

## 📦 Estructura de Capas

### 1. Presentation Layer (Controller)
```typescript
MenuManagementController
├── Role Endpoints (5)
│   ├── getAllRoles()
│   ├── getRoleById()
│   ├── createRole()
│   ├── updateRole()
│   └── deleteRole()
├── Permission Endpoints (5)
│   ├── getAllPermissions()
│   ├── getPermissionById()
│   ├── createPermission()
│   ├── updatePermission()
│   └── deletePermission()
├── Option Endpoints (6)
│   ├── getAllOptions()
│   ├── getOptionTree()
│   ├── getOptionById()
│   ├── createOption()
│   ├── updateOption()
│   └── deleteOption()
└── Role Permission Endpoints (6)
    ├── getRolePermissions()
    ├── assignPermission()
    ├── bulkAssignPermissions()
    ├── updateOptionPermissions()
    ├── deleteRolePermission()
    └── deleteAllOptionPermissions()
```

### 2. Business Logic Layer (Service)
```typescript
MenuManagementService
├── Role Operations
│   ├── getAllRoles()
│   ├── getRoleById()
│   ├── createRole()
│   ├── updateRole()
│   └── deleteRole()
├── Permission Operations
│   ├── getAllPermissions()
│   ├── getPermissionById()
│   ├── createPermission()
│   ├── updatePermission()
│   └── deletePermission()
├── Option Operations
│   ├── getAllOptions()
│   ├── getOptionTree()
│   ├── getOptionById()
│   ├── createOption()
│   ├── updateOption()
│   └── deleteOption()
└── Role Permission Operations
    ├── getRolePermissions()
    ├── assignPermission()
    ├── bulkAssignPermissions()
    ├── updateOptionPermissions()
    ├── deleteRolePermission()
    └── deleteAllOptionPermissions()
```

### 3. Data Access Layer (Repositories)
```typescript
RoleRepository
├── findAll()
├── findById()
├── save()
├── update()
├── delete()
├── findRoleByUserId()
└── findRoleMinimumByUserId()

PermissionRepository
├── findAll()
├── findById()
├── save()
├── update()
├── delete()
└── findByIds()

OptionRepository
├── findAll()
├── findById()
├── save()
├── update()
├── delete()
├── findByParentOption()
└── findRootOptions()

RolePermissionOptionRepository
├── findByRoleId()
├── findByRoleAndOption()
├── findByRoleOptionAndPermission()
├── save()
├── saveAll()
├── delete()
├── deleteByRoleAndOption()
└── deleteByRoleOptionAndPermission()
```

### 4. Data Transfer Objects
```typescript
DTOs
├── AssignPermissionDto
├── BulkAssignPermissionsDto
└── UpdatePermissionsDto

ViewModels
├── PermissionViewModel
├── OptionViewModel
├── RoleDetailViewModel
├── RolePermissionDetailViewModel
├── RoleWithPermissionsViewModel
└── OptionTreeViewModel
```

## 🔗 Relaciones entre Entidades

```
┌─────────────────┐
│   RoleEntity    │
│  (roles table)  │
│                 │
│ • id            │
│ • name          │
│ • startLevel    │
│ • state         │
└────────┬────────┘
         │ 1
         │
         │ N
         ▼
┌──────────────────────────────┐
│ RolePermissionOptionEntity   │
│ (role_permission_options)    │
│                              │
│ • id                         │
│ • company                    │◄────── Multi-company support
│ • state                      │
└────┬──────────┬──────────────┘
     │ N        │ N
     │          │
     │ 1        │ 1
     ▼          ▼
┌─────────────┐ ┌─────────────────┐
│ Permission  │ │  OptionEntity   │
│   Entity    │ │ (options table) │
│             │ │                 │
│ • id        │ │ • id            │
│ • name      │ │ • name          │
│ • state     │ │ • icon          │
└─────────────┘ │ • uri           │
                │ • order         │
                │ • parentOption  │◄─── Hierarchical structure
                │ • state         │
                └─────────────────┘
```

## 🎯 Inyección de Dependencias

```
InversifyJS Container
│
├── Controllers
│   └── MenuManagementController
│       └── inject: MenuManagementService
│
├── Services
│   └── MenuManagementService
│       ├── inject: RoleRepository
│       ├── inject: PermissionRepository
│       ├── inject: OptionRepository
│       └── inject: RolePermissionOptionRepository
│
└── Repositories
    ├── RoleRepository
    │   └── inject: Connection
    ├── PermissionRepository
    │   └── inject: Connection
    ├── OptionRepository
    │   └── inject: Connection
    └── RolePermissionOptionRepository
        └── inject: Connection
```

## 🔐 Security Flow

```
Request
   │
   ▼
┌─────────────────────┐
│ JWT Token Present?  │──NO──► 401 Unauthorized
└──────────┬──────────┘
           │ YES
           ▼
┌─────────────────────┐
│  Validate Token     │──INVALID──► 401 Unauthorized
└──────────┬──────────┘
           │ VALID
           ▼
┌─────────────────────┐
│ Extract User Info   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Process Request     │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Check Company       │──MISMATCH──► 403 Forbidden
└──────────┬──────────┘
           │ MATCH
           ▼
┌─────────────────────┐
│ Return Response     │
└─────────────────────┘
```

## 📊 Data Transformation Flow

```
Database Entity (TypeORM)
         │
         ▼
    ┌────────┐
    │ Mapper │
    └───┬────┘
        │
        ▼
   ViewModel (DTO)
        │
        ▼
   JSON Response
        │
        ▼
     Client
```

## 🔄 RxJS Observable Pattern

```typescript
// Example flow
Observable<Entity>         // From Repository
    │
    ▼
  map()                    // Transform with Mapper
    │
    ▼
Observable<ViewModel>      // To Controller
    │
    ▼
subscribe()               // Send HTTP Response
```

## 🛠️ Technology Stack

```
┌─────────────────────────────────┐
│         TypeScript 4.x          │ Language
├─────────────────────────────────┤
│          Express.js             │ Web Framework
├─────────────────────────────────┤
│         InversifyJS             │ Dependency Injection
├─────────────────────────────────┤
│            RxJS                 │ Reactive Programming
├─────────────────────────────────┤
│          TypeORM                │ ORM
├─────────────────────────────────┤
│           MySQL                 │ Database
├─────────────────────────────────┤
│        JWT (jsonwebtoken)       │ Authentication
└─────────────────────────────────┘
```

## 📈 Scalability Considerations

### Horizontal Scaling
- Stateless service design
- JWT for authentication (no sessions)
- Database connection pooling

### Performance Optimization
- Eager/Lazy loading with TypeORM
- Query optimization with indexes
- Caching layer ready (CacheService available)

### Maintenance
- Clear separation of concerns
- Single Responsibility Principle
- Open/Closed Principle
- Dependency Inversion Principle

---

**Arquitectura diseñada para**:
- ✅ Escalabilidad
- ✅ Mantenibilidad
- ✅ Testabilidad
- ✅ Extensibilidad
- ✅ Seguridad
